actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
              url: 'http://127.0.0.1:8000/'
              depends: PhpBrowser
        - Db:
              dsn: 'mysql:host=localhost:9906; dbname=simple_blog_db'
              user: 'tester'
              password: 'tester'
#              dump: 'tests/_data/simple_blog_db.sql'
#              cleanup: true # run populator before each test
#              populator: 'mysql -u $user -h $host $dbname < $dump'
